[project]
name = "backend"
version = "0.1.0"
description = "Flask backend for VibeKit Studio"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "asyncpg>=0.29.0"
]
